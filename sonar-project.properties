# SonarQube configuration for val-builder-api
# Exclude all test files and folders from analysis
sonar.exclusions=**/*.Tests.cs,**/Tests/**,**/*.Test.cs,**/test/**,**/tests/**

# Optional: Set project key and name
sonar.projectKey=val-builder-api
sonar.projectName=val-builder-api

# Optional: Set source encoding
sonar.sourceEncoding=UTF-8
